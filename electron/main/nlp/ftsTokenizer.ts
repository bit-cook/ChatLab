/**
 * FTS tokenizer — re-exported from @openchatlab/node-runtime.
 *
 * Kept to avoid changing internal import paths in Electron.
 */

export { tokenizeForFts, tokenizeQueryForFts } from '@openchatlab/node-runtime'
